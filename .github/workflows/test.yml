name: Test

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Hello
        run: echo "Hello World - test passed"
      - name: Check Node
        run: node --version
      - name: Check npm
        run: npm --version
